import ListingsLayout from "../../components/ListingsLayout";

/**
 * TagsLayout lays out the tag collection view.
 * @class
 * @child
 */
export default class TagsLayout extends ListingsLayout {
  constructor(props) {
    super(props, 'tags');
  }
};
